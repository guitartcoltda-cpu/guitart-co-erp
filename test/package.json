{
  "name": "salao-erp-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Testes automatizados do Salão ERP (comissionamento, etc.). Rodar com: npm install && npm test",
  "scripts": {
    "test": "node comissoes.test.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
